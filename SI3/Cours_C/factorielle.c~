long factorielle(int n){
  long fact = 1;
  for (int i=2 ; i<=n)
    fact=fact*i;
  return fact;
}

int main(){
  int n;
  scanf("%d",&n);
  assert(n>=0);
  printf("ld",factorielle(n));
  return EXIT_SUCCESS;
}
