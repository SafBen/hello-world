#include <stdlib.h>
#include <stdio.h>

int main(void){
  int n;
  char c;
  scanf("n=%d\n",&n);
  while ( (c=getchar() != EOF ){
      c+=n;
      if (c>'Z')
	c-=26;
    }
    putchar(c);
    return EXIT_SUCCESS;
    }
